# Scripts map

- `ui/`: desktop shell bootstrap and shortcuts.
- `engine/`: Python engine lifecycle and watchdog.
- `data/`: one-shot collectors/snapshot utilities.

Use absolute script paths from repo root in docs and operations.
