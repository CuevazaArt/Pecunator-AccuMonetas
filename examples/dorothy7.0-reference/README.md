# Dorothy7.0 Reference

Legacy standalone sample kept for historical context.

- Not imported by runtime.
- Not used by Flutter hubs directly.
- Credentials file is intentionally a template (`config.example.py`).

Use this folder only to compare strategy semantics during migration discussions.
