# Enhanced examples intake

This directory replaces the previous `exampleJV_enhanced` location.

Use it as a temporary intake zone for improved bot prototypes before they are
ported into `runtime/modules/bots/*` and documented in `docs/CHANGELOG.md`.
