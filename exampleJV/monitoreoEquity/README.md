## monitoreoEquity (legacy reference)

The original script in this folder was integrated into PecunatorCore runtime/UI:

- Spot equity conversion to base asset
- Rolling average / historical max-average
- Real-time visibility in Spot details

Legacy script files with embedded credentials and CSV artifacts were removed.
Use runtime APIs and the desktop UI instead of standalone scripts.
